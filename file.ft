: foo
  dup 10 < if
    100 +
  else
    10 +
  endif
;

: double dup + ;

: abs dup 0 < if -1 * endif ;

: power dup 1 = if drop else over swap 1 - power * endif ;

2 3
thread power
thread foo
thread abs

end